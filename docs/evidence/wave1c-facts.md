# Wave 1c facts — measured on the tree this file describes

Base: `0bc4d7f` (main, after PR #2). Tree measured: the commits on
`v2.1-wave1c-governance-defects`. Every line below was run on that tree; a figure
carried forward from wave 1b would be a different tree's number, so the wave-1b
rows in `docs/evidence/wave1b-facts.md` are left exactly as they were and are not
restated here.

| # | fact | measured |
|---|---|---|
| W1 | the new lane against the **base** tree, before any fix | `8 failed, 1 passed`. The one pass is the CONTROL case (`test_c1_control_a_filled_in_install_adds_no_slot_failure`), green at base by design; it stops meaning anything if it ever goes red. Raw output: private on this host (`.superpowers/…/lane-1c-red-at-base.log`), so the PR body carries the same 9 lines inline — a reader of the clone cannot reach the file |
| W2 | fresh install into a throwaway git repo | `== 21/25 checks passed, 4 skipped ==`, rc 0. Wave 1b measured `20/24` on its own tree; the difference is exactly one check (`unfilled template slots`), which PASSes and adds **no** new skip, and the test that pins this count is the tripwire that proves it |
| W3 | the same repo after `--migrate` | `== 22/25 checks passed, 3 skipped ==`, rc 0 |
| W4 | this repository's own two coverage walks, as of `affa2a4` | Both report FAIL, and the honest form of this row is the command plus the invariant, not a count: `python .ai/scripts/sync_verify.py`. `path coverage` counts every commit touching `docs/evidence/*`, and **publishing this file is such a commit**, so any number written here is already one behind the tree that carries it — measured `4 uncovered of 13` two commits ago and `13 protected touches` here. The invariant does not rot: with the stage record `pending`, `is_accepted()` gives it no editable list, so nothing under `docs/evidence/` is covered and rc is 1. The earlier version of this row claimed `26/26` with `8 protected touches`, which described the tree before this file existed |
| W5 | the project's own suite | `489 passed, 5 skipped` (same 5 POSIX-only skips). Was `480 passed, 5 skipped` on wave 1b's tree; the 9 added cases are this lane |
| W6 | why `path coverage` fell from 33 to 9 | Not a regression: `scripts/*` and `templates/*` left `protected_paths`. The user's ruling was that a record in this repository's own runtime must not certify what gets published, and registering the release face there did exactly that. The 9 left are this repository's own evidence commits |
| W7 | the installer, given a repo whose local identity is `Ada Lovelace <ada@example.com>` | `AGENTS.md` line 46 became `` - Commit identity: `Ada Lovelace <ada@example.com>` `` and `ROLE_POLICY.md` line 3 became `> Adopted: 2026-09-22 11:45:35 (+10:00), by \`init_sync.py\` (cross-harness-sync 2.1.0).` — read from the repository's own config, not a constant, and not the machine's global one |
| W8 | the installer, given a repo with **no** identity set — the original claim is withdrawn | It first read: exit 0, a named `[WARN]`, the slot replaced by a sentence. That was measured under the test harness's hermetic HOME, which hides the global config file, so it described a condition no real user meets. In a plain `git init` on this host `git config user.name` answers `Beichen Li` from the machine's global config, and the installer wrote that into a stranger's `AGENTS.md` — the fix for "the identity slot is never filled" reintroduced the exact address this project exists to keep out. Now it reads `--local` only, and `test_c3_a_global_identity_is_not_written_into_a_strangers_repo` injects a `GIT_CONFIG_GLOBAL` so the fallback cannot return unreported |
| W9 | a GBK-encoded `AGENTS.md` | crashed the installer with `UnicodeDecodeError` in the first cut of this change — caught by the pre-existing `test_a_gbk_agents_md_does_not_crash_the_install`, not by anything I wrote. Now left untouched and named: `AGENTS.md: unreadable (UnicodeDecodeError), so its slots stay as found`. The predicate does not consult `exists()`, which on this host answers `False` for a file it merely cannot read |
| W10 | `--force` after the installer filled its own slots | The first cut broke the upgrade path: `KEEP (edited): …ROLE_POLICY.md - pass --clobber to overwrite`, i.e. every install would look hand-edited forever and `--force` would refresh nothing. Fixed by treating exactly the filled span as free (`installer_slot_lines()`), which is why W10 exists as a test at all |
| W11 | the two things this stage found in its own governance text | `.ai/state/ROLE_POLICY.md` shipped `Adopted: <YYYY-MM-DD HH:MM:SS> … by <who>` and an instruction-shaped section 7 while its digest was pinned — the pin proved nobody had looked, not that the document was real. And R3 required a different model family at T2/T3 while R5 said family is recorded and never gates, so the shipped policy contradicted itself |
| W12 | what the digest pin did when changed | One edit (`03f80ef0…` → `1cabec53…`) and `role policy integrity` stayed green; with the old value left in place it prints `[FAIL] … digests to … but config pins …: the governance document changed without the config edit that makes the change visible`. The mechanism works; it was pointed at a document nobody had finished |
| W13 | the review of this increment, and what it found | A fresh-context subagent with no shared history reproduced the red run (`8 failed, 1 passed` on a `0bc4d7f` worktree), confirmed fresh `21/25` and the suite `489 passed, 5 skipped`, and returned **1 Critical, 7 Important, 2 Minor**. The Critical is mine again: W4's `26/26`/`8 touches` described the tree before this file was committed, i.e. the same rule `AGENTS.md` states. It also found the identity fallback in W8 (below), that `scripts/init_sync.py:2162` still tells users `git add -A && git commit`, that `reference.md` lines 72/307/310 still require a cross-family reviewer though the release record names that file in scope, that `7 failed` in the release record should read `8`, that `22/25` has no tripwire test, and that **nothing now enforces the two-face split** — removing `scripts/*` and `templates/*` from `protected_paths` deleted the only walk over shipped paths, so an unauthorised edit to `scripts/foo.py` under a runtime record prints no FAIL anywhere |
| W14 | whether this review was cross-family | Not verifiable from this host's logs, so it is not claimed. `~/.qoder/logs/sessions/F--Papers-and-Projects-cross-harness-sync/` holds one segment for the session and every `"model"` field in it is `qfmodel`, with no per-agent attribution, so neither the reviewer's family nor its tier can be read out. Recorded as `NOT confirmed` in the authorization rather than asserted: R5 says an unknown field is never guessed, and the claim that `general-purpose` sits in a different family from the main agent is a memory of an earlier host state, not a measurement of this one |

| W15 | disposition of the review | Five findings closed on this tree (`19fcfcb`, `8b2b821`): the emitted `git add -A`, three `reference.md` requirements, the `7 failed` count, the CONTROL argument, three unsupported "same model family" assertions. Both replacement assertions were shown red against the unfixed text first — `reference.md` on each needle, `scripts/init_sync.py:2170` on an emitted-string scan that ignores comments, because two comments in those files quote the command to explain why it is wrong. Lane `11 passed`, suite `491 passed, 5 skipped`, `path coverage: 2 uncovered of 10 protected touches` with rc 1 while the record stays `pending`. Open: a tripwire for `22/25`, and any check at all over the release face |
| W16 | the release gate, on a throwaway repo and on this one | New check, cases in `tests/test_lane_1c_release_gate.py` (this row carried a case count and got it wrong twice; the file is the authority): no record → FAIL naming each touch; accepted record → PASS; **pending record → still FAIL**, the case the design rests on; unconfigured → `SKIP(no-release-paths)`; directory absent → FAIL; a non-UTF-8 record → FAIL naming it unreadable. Counts for the published documents, measured not inferred: fresh `== 21/26 checks passed, 5 skipped ==`, after `--migrate` `== 22/26 checks passed, 4 skipped ==`, suite `498 passed, 5 skipped` at `065e9af7` — `python -m pytest tests/ -n 8 -o addopts= -q` is the command, W20 the current figure; the migrated figure now has a tripwire. This row first read `13 uncovered of 13`, which was two commits stale on arrival — see W17. The release face carries its own anchor because sharing the runtime window reported 46 of 46, most older than the concept |
| W17 | the review of the gate, and the fifth count of mine it caught | A fresh-context reviewer attacked the new check with constructed mutants and concrete configs, and reported 5 Critical, 5 Important, 2 Minor — all reproduced by its own commands. Confirmed on this host before any fix: my release record's `governance` block failed to parse at all, because I folded a value across lines and §6 makes a line without `: ' fatal, so `fields` came back `{}` and the record could **never** be accepted; it had no `## Editable files` section either, and after adding one, three packed comma-separated bullets parsed as three patterns rather than eleven, since the reader takes one entry per line. It also caught that `W16`'s figure was stale on arrival — the fifth time this stage published a number from a tree it had already left behind, and the reason W4/W16 are now written as a command plus an invariant. Still open in the gate itself: a misspelled `release_paths` or an anchor at current HEAD reports `PASS, 0 touches covered`; `release_authorizations_dir` skips the repo-relative shape check and read a record from outside the repository; a bullet of `*` would authorise the whole release face forever. Mutant evidence: deleting the check reddens all six cases, and only `test_c4_3` dies if pending is treated as accepted — so the load-bearing case is real. What is fixed here is the record and these two rows |
| W18 | the four holes the gate's review named, each written as a red test first | `tests/test_lane_1c_release_gate.py` grew C4-7…C4-10 and all four failed before the fix and pass after: a misspelled `release_paths` booked `PASS, 0 touched`; an anchor equal to the tip emptied the range and booked the same PASS; `release_authorizations_dir` accepted `../elsewhere-authorizations` (now rc 2, the refusal `authorizations_dir` already had); and a quiet window whose only record could not decode reported green while naming that record unreadable in its own evidence string. Two notes on how. The predicate is shared in name only — wiring it into `path coverage` reddened two settled tests that anchor at the tip on purpose and expect a PASS, so the runtime walk keeps the hole and `.ai/state/DECISIONS.md` says so, rather than a check widening its own reach because other code was nearby. And one finding is recorded instead of closed: editable bullets are `fnmatch` patterns whose `*` crosses `/`, so a single `*` in a release record would cover the whole published surface forever under the union rule. Newly pointed at shipped files by this PR, not invented by it. The three new keys are documented in `reference.md` and present in `templates/sync_config.json`, since a release face nobody can discover is a release face nobody registers |
| W19 | the acceptance step, run as a dry-run, and the defect it could not pass | Flipping the verdicts is the stage's last action, so it was tried on the working tree and reverted before any commit. Three measurements, each from one `python .ai/scripts/sync_verify.py` run: nothing accepted → `path coverage: 30 uncovered of 39`; this record accepted with wave 1b still open → `5 uncovered` plus `swarm boundary: 2 concurrent accepted`; this record accepted and wave 1b **retired** (`verdict: retired`) → `11 uncovered`. The 11 are those 5 plus six `docs/evidence/wave1b-facts.md` touches, so retiring a finished stage retracted the authorization that covers its own commits: `verdict` was answering "is this an authority" and "is this stage live" at once, and no value of one field can be yes to the first and no to the second. `status: open\|closed` now answers liveness alone, read by `swarm boundary` and by `checkpoint --review-prompt`, deliberately not by the coverage walk.   Five cases added to `tests/test_swarm_boundary.py` and two to `tests/test_review_prompt.py` for the matching half in that command; FIVE of the seven were red before the fix — W22 caught this row claiming three, which was the wrong number by two and is corrected here rather than quietly rewritten. Rehearsed on this tree and reverted (both records accepted, `status: closed` on wave 1b's): `python .ai/scripts/sync_verify.py` exits 0 with no FAILED line, `== 27/27 checks passed ==`, `path coverage: 39 protected touches covered`, `release authorization: 34 release-face (commit, path) pairs covered`. Those two counts include the commits this row is written into, so a later run prints bigger ones; what is claimed is that every touch the walk counts is covered, not the totals |
| W20 | the two faces of one sentence: a comma-packed bullet grants one path | The split above forced the same question about this stage's own record, and the answer was not comfortable. `.ai/scripts/*` went into `protected_paths` while the record that had to cover it listed `.ai/scripts/sync_verify.py` and `.ai/scripts/ai_common.py` on one bullet twice over — the parser takes each bullet's FIRST backtick span as its pattern, so that line granted `sync_verify.py` twice and nothing to `ai_common.py`, and `checkpoint.py` was never listed at all. Same defect W17 caught in the release record, same silent shape: an enumeration that reads as three paths and matches one. Wave 1b's install bullet is the third instance and the one left alone on purpose — splitting it would activate the `.ai/state/*.md` and `.ai/templates/*` patterns its own text disclaims, widening an accepted record's grant after the fact to fix a sentence, which is the trade this stage exists to refuse |
| W21 | the counts for this increment, on this tree | `python -m pytest tests/ -n 8 -o addopts= -q` → `516 passed, 5 skipped` (seven cases new: five lifecycle, two review-prompt). `python .ai/scripts/sync_verify.py` before the acceptance commit: `== 25/27 checks passed ==`, `FAILED: path coverage, release authorization` — `pending` records cover nothing, which is the design (C4-3) and not a green to be had. Fresh-install and `--migrate` check counts unchanged (`21/26, 5 skipped` / `22/26, 4 skipped`), because this fix altered a check's rule and not the check list |
| W22 | two fresh-context passes on the lifecycle split and the gate, and what they broke | Dispatched in parallel, read-only, told to attack with mutants and throwaway clones rather than agree. Pass A on the split: 3 Important, 5 Minor — a false `status: closed` costs its author NOTHING (the asymmetry my own docstring denied: a false `verdict` at least loses coverage); `checkpoint --review-prompt` printed a closed record's absence as "none of them is an accepted authorization" and never named the spent record at all; `is_live_stage` had zero callers, so the shared-predicate rule `is_accepted` enforces did not extend to liveness; shipped text said "only `swarm boundary` consults it" while `checkpoint` did too. Pass B on the gate: 3 Critical, 3 Important, 1 Minor — moving `release_window_start_commit` forward drops the commits an accepted record authorised and they then read as COVERED because nothing walks them (reproduced: `== 27/27 ==` over 43 pairs); an unreadable record in the release directory sat under a coverage PASS, named only inside its own evidence string; the glob refusal exists for release bullets only, while `.ai/state/DECISIONS.md` claimed "Scope: both record kinds". Every one of these was reproduced on this host before being accepted, and one of them was a count of mine again (W19's "three of the seven"; five were red) |
| W23 | dispositions, and what is still open after them | Closed on this tree, red-first: `_release_base_conflicts` requires every accepted release record to state `window_start_commit:` and refuses an anchor that has moved past it (C4-19, C4-20 red at `4e99840`; C4-22 is the CONTROL that stays green both ways); an unreadable release record is now a FAIL, not a PASS with a note (C4-18); a quiet window with nothing accepted is `SKIP(quiet-window-unanchored)` rather than a zero-count green (C4-21); `swarm boundary` calls the shared `is_live_stage`; the review prompt names closed records and no longer calls them non-accepted; the template, `reference.md` and the predicate's own docstring say plainly that closing is a self-report the boundary believes. Left open and recorded rather than fixed: an accepted release record never expires inside its window, so later shipped commits inherit it (the stale-grant rule, unchanged); a typo'd `release_paths` still yields `SKIP(no-release-paths)` at rc 0 with no unknown-key warning; the runtime walk keeps the empty-window hole and glob-tolerant bullets (W18); `authorizations_dir` resolves relative to the checkout in one reader and to `.ai/` in the other. Current figures, re-measured after every change: suite `521 passed, 5 skipped`; `python .ai/scripts/sync_verify.py` `== 25/27 checks passed ==` with `FAILED: path coverage, release authorization` while both records are `pending`; the acceptance rehearsal `== 27/27 ==` at rc 0 |

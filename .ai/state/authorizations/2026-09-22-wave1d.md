# Authorization — wave 1d, runtime face (this repository's own state)

> Issued: 2026-09-22 20:20 (+10:00) by the user, who said to continue and not to
> leave a mess behind. Recorded by qoder-cli (controller).
> Git commit at issuance: `aecd536` (head of `v2.1-wave1c-release-gate`, after wave 1c
> was accepted).

## Scope

Two things, both owed by wave 1c's own artifacts.

First: the deferred queue wave 1c points at does not exist publicly.
`.ai/handoff/NEXT_PROMPT.md` names "wave-1b minors: M-3, M-4, M-5, M-7..M-14" and
`CURRENT.md`, `TASK.md` and `BLOCKERS.md` publish the count ("14 minors") — and no
tracked file defines any of those ids. What exists is one clause in
`.superpowers/`, which `AGENTS.md` and `BLOCKERS.md` both declare unreachable to a
reader. `docs/evidence/wave1b-facts.md` §7 forbids exactly this: "an identifier it
does not define would be a dangling promise". The queue is therefore rewritten from
what the ledger actually says, item by item, and where the ledger lumps seven
findings into one clause that is published as lumped rather than dressed up as seven
resolvable rows.

Second: the shipped-code work wave 1c deferred and named — the governing-copy drift
check, the window-narrowing hole on the runtime walk, the uppercase-hex second
predicate, and the two `CHANGELOG.md` entries wave 1c left unwritten.

## Editable files

- `docs/evidence/wave1d-queue.md` — the rewritten queue, one row per item the
  ledger defines
- `docs/evidence/wave1d-facts.md` — this stage's measurements and the disposition
  of every finding its review raised
- `docs/evidence/wave1c-facts.md` — the correction that its own deferred row cited
  unresolvable ids
- `docs/release-authorizations/2026-09-22-wave1d-product-changes.md` — the
  release record this stage writes, governed by `protected_paths` so a verdict
  flip there needs an accepted runtime record naming it
- `docs/release-authorizations/2026-09-22-wave1c-product-changes.md` — its
  `status: open` → `closed` at acceptance. A closed record keeps covering the
  commits it was accepted for; leaving wave 1c live would put two live accepted
  records in one window, which is what `swarm boundary` exists to refuse, and
  editing its `verdict` instead would retract its own coverage (W19)
- `.ai/state/authorizations/2026-09-22-wave1c.md` — its `status: open` → `closed` in
  the same commit as its release-face twin above, for the same reason. Outside
  `protected_paths`, so no check requires listing it; it is listed because this
  stage edits it
- `.ai/state/CURRENT.md`
- `.ai/state/TASK.md`
- `.ai/state/BLOCKERS.md`
- `.ai/state/DECISIONS.md`
- `.ai/state/DECISIONS_INDEX.md`
- `.ai/state/authorizations/2026-09-22-wave1d.md`
- `.ai/state/authorizations/INDEX.md`
- `.ai/handoff/LATEST.md`
- `.ai/handoff/NEXT_PROMPT.md`
- `.ai/sync_config.json` — listed in case this stage had to widen a budget; the
  drift check deliberately adds no config key and the anchor stays where it is
- `.ai/scripts/sync_verify.py`
- `.ai/scripts/ai_common.py`
- `.ai/scripts/checkpoint.py`
- `.ai/runtime/WRITER_LOCK.json`
- `CHANGELOG.md` — the wave-1c release-document entries wave 1c recorded as owed
- `AGENTS.md`
- `CLAUDE.md`

## Boundary with the release face

`scripts/`, `templates/`, `tests/`, `README.md`, `SKILL.md` and `reference.md` ship
to other people and are authorised in
`docs/release-authorizations/2026-09-22-wave1d-product-changes.md`. Nothing here
grants them, and no wildcard appears in either list.

## Roles

- Executor: qoder-cli controller, inline, red-first per R4.
- Reviewer: two fresh-context qoder-cli subagent passes, the second dispatched at
  the first one's closures. Findings and dispositions are W1-W9 and X1-X8 of
  `docs/evidence/wave1d-facts.md`, and X1 is the case where a closure introduced a
  defect. Model family and tier are `NOT_REPORTED`: this host's log carries one
  model field for every segment, so no cross-family claim is made, and one of the
  two dispatched passes died in the service without reporting and was not repeated.

## Completion condition

`python .ai/scripts/sync_verify.py` reports no FAILED line on this tree once this
record is accepted. The governance window is not advanced this stage — the anchor
stays at `db091bd` — so every wave 1d touch has to be covered by this record's
enumeration rather than by a narrowed range. That is a decision, not a guard: what
this stage wrote into `_base_conflicts` binds only a LIVE accepted record that
DECLARES a base, and wave 1c's declares none (row Q14 of
`docs/evidence/wave1d-queue.md`), so nothing machine-side stops a later stage
anchoring past it. What the new guard does bind is this record: it declares
`window_start_commit: aecd536`, so once accepted, a later anchor advanced past that
commit is a FAIL until this stage is closed. Every item in
`docs/evidence/wave1d-queue.md` is either closed with a test that was red first,
closed as a coverage pin that says it could not be, or carries a row saying it was
left open and by whom.

## Stop boundary

No tag, no GitHub Release, no version bump, no merge of any PR, no push to `main`,
no change to the user's global git config. Never `git add -A`, never `git add -f`
the gitignored `.superpowers/`, never force-push, never rewrite published history.
Nothing in this record authorises editing `scripts/`, `templates/` or `tests/`.
The `.superpowers/` ledger is read for this queue and not published, copied, or
quoted beyond the item text the queue itself needs.

## Governance

```governance
tier: T2
executor: qoder-cli/controller
reviewer: qoder-cli general-purpose subagents, two fresh-context passes, the second at the first's closures; model family and tier NOT_REPORTED (unreadable from this host's logs, and one dispatch died unrepeated) -- no cross-family attestation (R5)
verdict: accepted
status: closed
window_start_commit: aecd536bc2e073e19d60ebc984ba614f1a6fc549
red_before_green: true
user_authorized: true
```

# Authorization — wave 1c, runtime face (this repository's own state)

> Issued: 2026-09-22 11:58 (+10:00) by the user, who chose to merge PR #2 and start
> wave 1c, ruled on the R3/R5 conflict, and then ruled that a release-face change
> may not be authorised here.
> Recorded by: qoder-cli (controller).
> Git commit at issuance: `0bc4d7f54a37a0cf0c15bc999af42eac320d073e` (main, after
> the PR #2 merge).

## Scope

The install side only: this repository's own governance text, its state records,
its evidence file, and its config. Executing spec 10.D's successor — retiring the
defects the dogfood found in the files this repo lives in.

## Editable files

- `docs/evidence/wave1c-facts.md` — the measurements below, on this tree
- `docs/release-authorizations/2026-09-22-wave1c-product-changes.md` — governed
  now by `protected_paths`, so flipping a release verdict to `accepted` is itself a
  touch that needs an accepted runtime record. Without this line the authority file
  was the one thing in the arrangement nobody could be stopped from editing
- `.ai/state/CURRENT.md`
- `.ai/state/TASK.md`
- `.ai/state/BLOCKERS.md`
- `.ai/state/DECISIONS.md`
- `.ai/state/DECISIONS_INDEX.md`
- `.ai/state/authorizations/2026-09-22-wave1c.md`
- `.ai/state/authorizations/INDEX.md`
- `.ai/handoff/LATEST.md`
- `.ai/handoff/NEXT_PROMPT.md`
- `.ai/state/ROLE_POLICY.md` (the installed copy; the template is release face)
- `.ai/runtime/WRITER_LOCK.json`
- `AGENTS.md`
- `CLAUDE.md` — this repository's instructions, not `templates/AGENTS.md`
- `.ai/sync_config.json` — now governed, because it carries `release_paths` and the
  window anchor: dropping `scripts/*` from the first, or advancing the second, silently
  narrows the gate to whatever it then ignores
- `.ai/scripts/sync_verify.py`
- `.ai/scripts/ai_common.py`
- `.ai/scripts/checkpoint.py` — the three copies that execute here, newly inside
  `protected_paths`, named one per line because the parser reads a bullet's FIRST
  backtick span as its pattern and grants nothing to a second path on the same
  line. Two wave-1b commits touched `ai_common.py` and `checkpoint.py`, and only a
  named path covers them. The drift check that would compare these to `scripts/` is
  still open, so this stage copies them by hand and says so.
- `.ai/state/authorizations/2026-09-22-wave1b-dogfood.md` — carries
  `status: closed` in the commit that accepts this one. NOT a changed `verdict`:
  that record is what covers wave 1b's own protected touches, and a stage finishing
  does not un-authorize the commits it was accepted for (see W19)

## Boundary with the release face

`scripts/` and `templates/` ship to other people. They are authorised in
`docs/release-authorizations/2026-09-22-wave1c-product-changes.md`, and this
stage removed them from `protected_paths` on purpose: leaving them there meant a
record from this repository's own runtime was certifying what gets published,
which is the confusion the user named. The release face is guarded by `tests/`
and the review, not by this file.

## Roles

- Executor: qoder-cli controller (fixes implemented inline, red-first per R4).
- Reviewer: fresh-context subagents, none sharing history with the implementation.
  The first returned 1 Critical, 7 Important, 2 Minor (W13); the one that attacked
  the release gate returned 5 Critical, 5 Important, 2 Minor (W17), and W18 records
  what was closed from it. None of their model families could be read from this
  host's logs (W14), so nothing here may call any of them cross-family. A further
  pass on the lifecycle split (W19) is the remaining condition on `pending`.

## Completion condition

`python .ai/scripts/sync_verify.py` reports no FAILED line once this record is
accepted **and** the wave-1b dogfood record carries `status: closed` in the same
commit, with the release record accepted in the same tree.

Measured on this tree with the checks in place, before that commit exists:

- accepting this record alone left `[FAIL] path coverage: 5 uncovered of 39`
  (`0bc4d7f5`, `20242cb6`, `c4a9586c` against `.ai/scripts/ai_common.py` and
  `checkpoint.py`) and `[FAIL] swarm boundary: 2 concurrent accepted`
- retiring the wave-1b record by rewriting its `verdict` instead uncovered 11 —
  those 5 plus 6 `docs/evidence/wave1b-facts.md` touches that record does name.
  That is the defect W19, and `status` is the fix for it
- accepting the release record clears `release authorization` (34 release-face
  (commit, path) pairs across 18 files covered on that tree)

Those figures are the pre-`4e99840` tree's, which is the mistake this project's own
rule names: the two commits that followed added touches to both walks. Re-run the
command for this tree. Rehearsed on it, then reverted: both verdicts accepted plus
`status: closed` on wave 1b gives `[PASS] path coverage: 45 protected touches
covered`, `[PASS] release authorization: 43 release-face (commit, path) pairs
covered`, `[PASS] swarm boundary: 2 accepted … 1 of them live, 1 closed`,
`== 27/27 checks passed ==` at rc 0; reverting the three lines returns `== 25/27 ==`
with the two FAILED names. What is claimed is the invariant — no touch left
uncovered — not the totals.

The split bullets above clear the 5; `status: closed` clears the boundary without
reopening them. The `unfilled template slots` check this stage added to the
product now runs against this install, and this tree must satisfy it — rc 0, no
`[SKIP]`; `pin violation` and `role policy integrity` green against the re-pinned
digest; every figure in `docs/evidence/wave1c-facts.md` re-measured here.

## Stop boundary

No tag, no GitHub Release, no version bump, no merge, no push to `main`, no change
to the user's global git config. Never `git add -A`, never `git add -f` the
gitignored `.superpowers/`, never force-push, never rewrite published history.
Nothing in this record authorises editing `scripts/` or `templates/`.

## Governance

```governance
tier: T2
executor: qoder-cli/controller
reviewer: qoder-cli/general-purpose subagent, no shared context; model family and tier NOT confirmed from this host's logs (W14), so this is not a cross-family attestation
verdict: pending
status: open
red_before_green: true
user_authorized: true
```

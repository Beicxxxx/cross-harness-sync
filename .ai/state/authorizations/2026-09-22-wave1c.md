# Authorization — wave 1c, runtime face (this repository's own state)

> Issued: 2026-09-22 11:58 (+10:00) by the user, who chose to merge PR #2 and start
> wave 1c, ruled on the R3/R5 conflict, and then ruled that a release-face change
> may not be authorised here.
> Recorded by: qoder-cli (controller).
> Git commit at issuance: `0bc4d7f54a37a0cf0c15bc999af42eac320d073e` (main, after
> the PR #2 merge).

## Scope

The install side only: this repository's own governance text, its state records,
its evidence file, and its config. Executing spec 10.D's successor — retiring the
defects the dogfood found in the files this repo lives in.

## Editable files

- `docs/evidence/wave1c-facts.md` — the measurements below, on this tree
- `.ai/state/CURRENT.md`, `.ai/state/TASK.md`, `.ai/state/BLOCKERS.md`,
  `.ai/state/DECISIONS.md`, `.ai/state/DECISIONS_INDEX.md`
- `.ai/state/authorizations/2026-09-22-wave1c.md`, `.ai/state/authorizations/INDEX.md`
- `.ai/handoff/LATEST.md`, `.ai/handoff/NEXT_PROMPT.md`
- `.ai/state/ROLE_POLICY.md` (the installed copy; the template is release face),
  `.ai/sync_config.json` (`role_policy_sha256`, `protected_paths`),
  `.ai/runtime/WRITER_LOCK.json`
- `AGENTS.md`, `CLAUDE.md` — this repository's instructions, not `templates/AGENTS.md`

## Boundary with the release face

`scripts/` and `templates/` ship to other people. They are authorised in
`docs/release-authorizations/2026-09-22-wave1c-product-changes.md`, and this
stage removed them from `protected_paths` on purpose: leaving them there meant a
record from this repository's own runtime was certifying what gets published,
which is the confusion the user named. The release face is guarded by `tests/`
and the review, not by this file.

## Roles

- Executor: qoder-cli controller (fixes implemented inline, red-first per R4).
- Reviewer: one fresh-context subagent, no shared history. It returned 1 Critical, 7
  Important, 2 Minor (W13); only the identity fallback is closed here, so `pending`
  stays. Its model family could not be read from this host's logs (W14), so nothing
  here may call the review cross-family.

## Completion condition

`python .ai/scripts/sync_verify.py` reports `== 26/26 checks passed ==` — the
`unfilled template slots` check this stage added to the product now runs against
this install, and this tree must satisfy it — rc 0, no `[SKIP]`;
`pin violation` and `role policy integrity` green against the re-pinned digest;
every figure in `docs/evidence/wave1c-facts.md` re-measured here.

## Stop boundary

No tag, no GitHub Release, no version bump, no merge, no push to `main`, no change
to the user's global git config. Never `git add -A`, never `git add -f` the
gitignored `.superpowers/`, never force-push, never rewrite published history.
Nothing in this record authorises editing `scripts/` or `templates/`.

## Governance

```governance
tier: T2
executor: qoder-cli/controller
reviewer: qoder-cli/general-purpose subagent, no shared context; model family and tier NOT confirmed from this host's logs (W14), so this is not a cross-family attestation
verdict: pending
red_before_green: true
user_authorized: true
```
